/* ============================================================
   main.js —— ES Module 入口
   ────────────────────────────────────────────────────────────
   所有模块通过 import/export 显式声明依赖。
   app.js 为主控制器，导入并初始化所有子模块。
   ============================================================ */
import './app.js';
